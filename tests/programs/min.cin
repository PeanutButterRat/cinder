// 1
fn main() -> i32 {
    let minimum: i32 = max(12, 1, 3);
    print minimum;
    return 0;
}

fn max(a: i32, b: i32, c: i32) -> i32 {
    if a <= b and a <= c {
        return a;
    } 
    if b <= c {
        return b;
    }
    return c;
}
