// 3
fn main() -> i32 {
    let maximum: i32 = max(1, 2, 3);
    print maximum;
    return 0;
}

fn max(a: i32, b: i32, c: i32) -> i32 {
    if a >= b and a >= c {
        return a;
    } 
    if b >= c {
        return b;
    }
    return c;
}
